#include<iostream>
#include<string>

int main()
{
    std::string a="bhavya", b="bhavya";
    if(a==b)
    {
        std::cout<<"the strings are equal";
    }else{
        std::cout<<"the strings are not equal";
    }
}